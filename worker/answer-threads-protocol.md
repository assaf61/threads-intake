# פרוטוקול עונה-לחוטים (answer-threads) v1 | 11/06/2026

מטרה: לולאה דו-כיוונית. אסף לוכד חוטים (קול/טקסט/לינק) מהטלפון והמחשב אל
`C:\Users\owner\OneDrive - עלמא ניהול פרויקטים\Alma Mind\Alma.Threads\00-raw\inbox\`,
ו-Claude עונה **בתוך קובץ החוט** - כך שהתשובה מופיעה לאסף בפאנל "אחרונים" באפליקציית חוטים.

## צעדים

**0. רענן העשרה** (תמלולים עדכניים לפני קריאה):
הרץ `python "C:\Users\owner\OneDrive - עלמא ניהול פרויקטים\OD - Alma\IT\AI\AI - מקומי\threads-intake\worker\enrich.py"` והמתן לסיום.

**1. אתר מועמדים:** קבצי `*.md` ב-inbox שבהם:
- frontmatter עם `type: capture`
- אין שדה `answered_at`
- יש תוכן לקריאה (גוף טקסט או `## תמליל`)

**2. החלט אם לענות.** ענה רק אם החוט פונה אל Claude או מכיל שאלה/בקשה/הוראה
(סימנים: "?", "אני מבקש", "תודיע לי", "תבדוק", "מה דעתך", פנייה ישירה).
רעיון, לינק או תמונה בלי שאלה - **אל תענה ואל תסמן**; זה חומר לסבב הבשלות, לא שיחה.

**3. ענה append-only.** הוסף בסוף הקובץ:

```
## תשובה (Claude, YYYY-MM-DD HH:MM)
<תשובה תמציתית בעברית, ישירה, בלי קישוטים>
```

והוסף ל-frontmatter שורה: `answered_at: YYYY-MM-DD HH:MM`.
לעולם אל תשנה או תמחק טקסט קיים, ואל תיגע ב-`target_vault`.

**4. גבולות קשיחים:**
- עבודה כבדה (דוח, מחקר רשת, שינוי קבצים אחרים, החלטות פרויקט) - אל תבצע בריצה מתוזמנת.
  ענה: מה התשובה המהירה אם יש + "לביצוע מלא: פתח סשן ובקש <ניסוח מוכן>".
- כתיבה אך ורק בתוך תיקיית ה-inbox (וקובץ הלוג שלה). שום מחיקה.
- אל תענה לחוט שכבר מכיל `## תשובה`.
- כבד את `Alma.Threads\CLAUDE.md` (שמות קבצים אנגלית, תוכן עברית) ואת
  `Alma.R\00-system\protected-services.json` (אל תעצור שירותים).
- אם Gemini במגבלת קצב והחוט לא מתומלל - דלג עליו בשקט; הריצה הבאה תטפל.

**5. סכם:** אם ענית על חוט אחד לפחות ויש כלי התראה (PushNotification) - שלח שורה אחת:
"חוטים: עניתי על N - <רשימת שמות קצרה>". אם לא ענית על כלום - סיים בשקט, בלי התראה.

## רישום
הוסף שורת JSON ל-`worker\answer-log.jsonl`: `{"ts": "...", "answered": [שמות], "skipped": מספר}`.
